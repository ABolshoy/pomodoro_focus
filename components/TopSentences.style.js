import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  text: {
    fontSize: 20,
    alignSelf: "center",
    fontWeight: "bold",
    paddingHorizontal: 20,
    textAlign: "center",
    color: "#E6DCCD",
  },
});
